# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
build/
dist/
.pytest_cache/

# PyInstaller (packaging output)
*.spec.bak

# Bundled sidecar binaries (large; ffmpeg/yt-dlp downloaded per-platform)
binaries/

# Built frontend (generated by `npm run build` in ./frontend)
ngc7023/web/

# Frontend deps / build
frontend/node_modules/
frontend/dist/

# Run logs
*.log

# Local icon working files (PNG/SVG masters — the build only needs the assets
# already in frontend/public/branding/ and packaging/)
finalicon/
packaging/icon-concepts/
